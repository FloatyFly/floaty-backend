apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: floaty-backend
  namespace: floaty
  labels:
    app: floaty-backend
spec:
  selector:
    matchLabels:
      app: floaty-backend
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
