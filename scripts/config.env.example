# MySQL Backup Configuration
# Copy this file to config.env and adjust the values

# Database container name (as shown in 'docker ps')
DB_CONTAINER=floaty-mysql

# Database credentials
DB_NAME=floaty
DB_USER=root
DB_PASSWORD=your_password_here

# Backup directory (absolute path recommended)
# Consider using external storage (USB drive) for backups
BACKUP_DIR=/home/pi/floaty-backups/backups

# Retention policy - number of backups to keep
# Hourly backups: 168 = 7 days
# Daily backups: 30 = 30 days
RETENTION_COUNT=168

# Maximum age for backups (in days)
# Any backup older than this will be deleted regardless of RETENTION_COUNT
# This acts as a safety net when changing backup frequency
# Set to empty or comment out to disable age-based cleanup
MAX_AGE_DAYS=90

# Backup frequency (for reference, set in crontab)
# hourly: 0 * * * *
# daily:  0 2 * * *  (2 AM daily)
BACKUP_FREQUENCY=hourly
